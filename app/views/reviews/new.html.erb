<head>
  <!-- fontawsomeのコード -->
  <script src="https://kit.fontawesome.com/f0d31dc058.js" crossorigin="anonymous"></script>
  <!-- jQueryのコード -->
  <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1/jquery.min.js"></script>
  <!-- ratyのコード -->
  <%= javascript_pack_tag 'jquery' %>
</head>
<!-- 新規投稿ページ -->
<section class="page-section">
  <div class="container">
    <div class="row">
      <div class="col-lg-12 text-center">
        <h2 class="section-heading text-uppercase"><%= @post.product_name %>の口コミを投稿しよう！</h2>
      </div>
    </div>
  </div>
  <%= form_tag("/reviews/#{@post.id}/create") do %>
  <div class="form">
      <div class="form-body">
        <% @review.errors.full_messages.each do |message| %>
          <div class="alert alert-primary" role="alert">
            <%= message %>
          </div>
        <% end %>
      <!-- レーティングのHTML-->
        <h5 class="section-subheading">評価
        <div id="star-rating"></div>
        <input id="score" name="rating" type="hidden"></h5>

        <h5 class="section-subheading">口コミ</h5>
        <textarea name="content" placeholder="140文字以内で口コミを書こう！"><%= @review.content %></textarea>
        <input type="submit" value="投稿">
      </div>
  </div>
  <% end %>
</section>
<!-- 評価のjavascript -->
<script>
  $('#star-rating').raty({
    path: '/assets/',
    target:     '#score',
    targetKeep: true,
    targetType: 'number',
  });
</script>
